(ns sentimetrika.views.welcome
  (:require [sentimetrika.views.common :as common]
            [noir.content.getting-started])
  (:use [noir.core :only [defpage]]))

(defpage "/welcome" []
         (common/layout
           [:p "Welcome to web"]))
